export const metadata = {
  title: 'Contact',
  description: 'Get in touch with me for your next project or architectural challenge.',
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
