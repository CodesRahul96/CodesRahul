export const metadata = {
  title: 'Projects',
  description: 'Explore my featured web and mobile applications, built with React, Node.js, and modern architectural principles.',
};

export default function ProjectsLayout({ children }) {
  return <>{children}</>;
}
